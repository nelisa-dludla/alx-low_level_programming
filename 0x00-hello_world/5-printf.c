#include <stdio.h>

/* This program prints out characters */

/**
 * main - Entry point
 * printf - prints "with proper grammer, but the outcome is a piece of art,"
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammer, but the outcomr is a piece of art,");
	return (0);
}
